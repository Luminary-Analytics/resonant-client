# AI Employee client integration: paused handoff

**September 23, 2026: AI Employee work remains PAUSED by the user.**
The [consolidated product checkpoint](D:/Repos/Lumina_DO/SelfOrganizingNN/product/AI_EMPLOYEES_CHECKPOINT_2026_09_23.md)
records subsequent paid research, negative/control results and remaining work.
No added SONN learning value or qualified employee/router release is established.
The heartbeat remains paused. Documentation maintenance does not resume work,
spending or grants, and changes no native implementation or installed bundle.
The dated September 15/18 records below are historical.

## Historical September 15 implementation checkpoint

September 15, 2026: the user stopped implementation and requested a documentation
checkpoint to resume later. **Do not resume implementation or paid qualification
until the user resumes.** The associated `implement-sonn-ai-employees` heartbeat
is confirmed paused.

## Source status

The SONN task controller, explicit advice/coordination flow, employee task panel,
assignment inspector and scoped file-only specialist workers are integrated into
this checkout. They preserve durable request identities, shared root allowance,
unknown accounting, cancellation, epoch isolation and verified artifact handoffs.
See [SONN connection](sonn.md) for the implemented client contracts.

Changes came from `D:/Repos/Lumina_DO/sonn-client-ai-employees`, after SHA checks.
That directory is an isolated source candidate, not a Git worktree. Its
`AI_EMPLOYEE_INTEGRATION.json` is a historical integration receipt. This pause
documentation update changes docs in the real checkout; do not overwrite them
from the older candidate or change old receipt hashes to conceal that difference.
No native implementation source changed during this documentation update.

The native candidate's last full regression passed **3,351 tests / 3 skipped** in
167.20 seconds. Product-side checks used actual native subprocesses with scripted
providers, including restart/recovery and three-worker/six-call verified integration.
Synthetic signed routing releases exercise model selection, not learned benefit.
Browser panel checks are distinct from packaged desktop execution qualification.
The installed/running desktop bundle was not rebuilt or replaced; these additions
are source-only, not a new public release. Preserve the existing dirty checkout.

## Resume entry point and remaining work

The authoritative cross-repository handoff is
[AI Employee handoff](D:/Repos/Lumina_DO/SelfOrganizingNN/product/AI_EMPLOYEES_HANDOFF.md).
It records the PRD/backlog, product test evidence, exact frozen artifacts and
release gates. The early $6/12-call canary was a historical proposal; subsequent
research ran under standing authorization. See the consolidated checkpoint for
September 19 outcomes, spending and the current pause.

Next implementation seam: bind native file-action observations to their exact
originating model requests. `SonnWorkerController.claim_tool` currently sends the
tool operation/name/arguments digest, without that model-request attribution.
Investigation stopped before new changes. Read `sonn.py`, `sonn_tasks.py`,
`sonn_workers.py` and the product worker/orchestration/route services together.
Do not train every upstream call from final-node success, treat ordinary client
reports as independent trust, or feed private qualification grades into learning.

Automatic bounded advice/supervision, full-chain commercial policy, trusted host
enrollment, graphical execution controls and real G2/G3/G4 qualification remain
open. The existing panel and explicit consultations do not close these gates.
On resume, verify source hashes, process ownership and the intended source/bundle
before integration or testing. Do not use a stale process ID or rebuild over a
running bundle. Follow the current product funding instructions after the user
resumes; preserve prior authorization without treating this review as a restart.
