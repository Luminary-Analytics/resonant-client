# Resonant Client v0.9.11

## Kimi tool-history compatibility

- Keeps separate legacy tool turns when deterministic call IDs are reused.
- Deduplicates only native Kimi tool-call groups that carry a real response ID.
- Prevents orphaned `tool` messages that Moonshot rejects with HTTP 400.
- Preserves compatibility when an existing session switches from Ollama or another backend to Kimi.

## Validation

- Live Moonshot API smoke accepted a repeated legacy tool-call ID conversation.
- The reported 1,220-turn saved session now serializes with zero orphan tool results.
- Full Python test suite passed: 2,761 passed and 2 skipped.
- Ruff, clean PyInstaller build, bundle policy, and frozen executable version smoke passed.
